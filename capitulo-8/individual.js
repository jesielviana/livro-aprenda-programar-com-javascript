// Modularização CommonsJS
const { soma, subtracao } = require('./operacoes-commonjs');

// Utilizando as funções e constantes exportadas pelo módulo

console.log(soma(5, 3)); // Saída: 8
console.log(subtracao(10, 4)); // Saída: 6
